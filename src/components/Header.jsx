import Navigation from './Navigation';

const Header = () => {
  return (
    <header>
      <div className="nav-area">
        <Navigation/>
      </div>
    </header>
  );
};

export default Header;